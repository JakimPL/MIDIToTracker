[project]
name = "midi2xm"
version = "2.0.0"
description = "MIDI2XM - Convert MIDI files into FastTracker 2 tracker modules"
readme = "README.md"
requires-python = ">=3.13,<3.14"
authors = [{ name = "Jakim" }]
dependencies = [
    "mido>=1.3",
    "numpy>=2.1",
    "pydantic>=2.13.4",
    "pyyaml>=6.0",
    "audiotokenizer",
]

[project.scripts]
midi2xm = "midi2xm.cli:main"

[dependency-groups]
dev = [
    "black==26.5.1",
    "isort==8.0.1",
    "mypy==2.1.0",
    "pylint==4.0.6",
    "pylint-pydantic==0.4.1",
    "pytest==9.1.1",
    "types-pyyaml>=6.0",
]

[tool.uv.sources]
audiotokenizer = { path = "../AudioTokenizer", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["midi2xm"]

[tool.black]
line-length = 120
target-version = ["py313"]

[tool.isort]
profile = "black"
line_length = 120
known_first_party = ["midi2xm"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--import-mode=importlib"

[tool.mypy]
files = ["midi2xm"]
exclude = ["tests"]
python_version = "3.13"
strict = true
plugins = ["pydantic.mypy"]
warn_unused_ignores = true
ignore_missing_imports = true

[tool.pydantic-mypy]
init_forbid_extra = true
init_typed = true
warn_required_dynamic_aliases = true

[tool.pylint.main]
load-plugins = ["pylint_pydantic"]
fail-under = 9.9

[tool.pylint."messages control"]
disable = [
    "missing-module-docstring",
    "missing-class-docstring",
    "missing-function-docstring",
    "line-too-long",
    "too-few-public-methods",
]
